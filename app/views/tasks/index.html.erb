<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task %>:<%= task.content %></li>  <%# show タスク詳細とid　%>
  
  <%# link_toはRailsが提供するメソッドです。link_to 表示文字列, リンク先 という形式で書きます。
  表示文字列はidなので、task.idです。リンク先は、taskになっています。
  リンク先として、taskのようにモデルのインスタンス（レコード）を渡すと、自動的に
  そのインスタンスのshowアクションへとリンクされます。 %>
  <%  end %>
  
 <P><%= link_to '新規タスクの投稿', new_task_path %>newアクション</P>
  
</ul>

